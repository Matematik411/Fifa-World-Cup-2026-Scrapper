# --- Python / tool state (must NEVER live in this host-mounted repo) ---
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.coverage

# --- secrets ---
.env

# --- OS cruft ---
.DS_Store

# --- working data that gets large: kept out of git ---
# raw source pulls + every processed run pile up; regenerated by `./run.sh run`.
data/raw/
data/processed/

# output/ IS committed so GitHub Pages can serve the rendered reports.
# data/manual/ (curated research inputs) is committed for reproducibility.
